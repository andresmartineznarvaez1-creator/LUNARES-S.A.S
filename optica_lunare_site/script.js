
// Simple script placeholder - all action links go to WhatsApp.
// You can extend these functions to integrate real cart or backend.

document.addEventListener('DOMContentLoaded', function(){
  // Lazy images if needed - placeholder logic
  document.querySelectorAll('img').forEach(img=>{
    if(img.dataset.src) img.src = img.dataset.src;
  });
});
